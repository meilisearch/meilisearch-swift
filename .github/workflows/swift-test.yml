name: Swift

on:
  push:
    branches: [ master ]
  pull_request:
jobs:
  download: 
    runs-on: macos-latest
    steps:
    - name: Download  MeiliSearch
      run: curl -L https://install.meilisearch.com | sh
  build:
    runs-on: macos-latest
    needs: download
    env:
      DEVELOPER_DIR: /Applications/Xcode_11.5.app/Contents/Developer
    steps:
    - name: Run  MeiliSearch
      run: ls -la && ./meilisearch &
    - name: Run tests
      run: swift test -v
